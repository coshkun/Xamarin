<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="W02T01.Styles.Views.ThemePage">

    <!--<ContentPage.Resources>
        <ResourceDictionary>
            <Style x:Key="LabelStyle"  TargetType="Label">
                <Setter Property="TextColor" Value="DarkRed" />
                <Setter Property="FontSize" Value="24" />
                <Setter Property="HorizontalTextAlignment" Value="Center" />
            </Style>

            <Style x:Key="LabelFont" TargetType="Label"
                   BasedOn="{StaticResource LabelStyle}">
                <Setter Property="FontFamily" Value="Helvetica" />
                <Setter Property="FontAttributes" Value="Bold" />
            </Style>
            
        </ResourceDictionary>
    </ContentPage.Resources>-->

    <ContentPage.Content>
        <ScrollView>
            <StackLayout Style="{DynamicResource DefaultBackground}">
                <Label Style="{DynamicResource DefaultTitle}"></Label>
                <Editor Style="{DynamicResource DefaultContent}"></Editor>
                <Button Style="{DynamicResource DefaultButton}"></Button>
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>

</ContentPage>